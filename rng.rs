use std::rand;

fn main() {
	let a = [(rand::random::<uint>() % 100u) + 1u, (rand::random::<uint>() % 100u) + 1u, (rand::random::<uint>() % 100u) + 1u, (rand::random::<uint>() % 100u) + 1u ] ;
	//println!("{}", a[(rand::random::<uint>() % 3u) + 1u]);
	}