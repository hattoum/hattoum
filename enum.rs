
enum Sort{
		Greater,
		Less,
		}
fn wat(a: int, b: int) -> Sort {
	
	if a < b { Less }
	}
fn main(){
	let (x, y) = (5i, 6i) ;
	let sort = wat(x, y) ;
	//wat(x, y) ;
	if sort == Less {
	println!("Less"); }
	else {
	println!("Greater"); }
	
	}
	 bleh